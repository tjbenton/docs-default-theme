mixin json(obj, spacing)
  - var type = highlight_types[random(0, highlight_types.length)]
  +window({ escaped: JSON.stringify(obj, null, 2), lang: 'json'  })
  //- pre(class="#{prefix}-c-code-block agate", type="agate")
  //-   code.hljs.json!= highlight(, 'json')