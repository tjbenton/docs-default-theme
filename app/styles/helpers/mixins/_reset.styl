/// @author Tyler Benton
/// @page helpers/mixins
///
/// @description - This is to reset the margin, padding, and list-style to be none
///
/// @todo {!!} tylerb - Update this to allow for removing one or more of the values from here (ie. $margin: false)
///
/// @arg {boolean}
///
/// @markup {styl} (example="false") **Example:**
/// .foo{
///  reset(); // This will be extended onto %reset
/// }
/// .bar{
///  reset(false);
/// }
/// --------------------------------------------------
reset($extend = true)
  if $extend
    +cache('reset' + $media)
      reset(false)
  else
    list-style none outside none unless @list-style
    margin-left 0 unless @margin-left
    margin-right 0 unless @margin-right
    padding 0 unless @padding